«Для тестового задания я использовал LocMemCache — это встроенный в Django кэш в памяти.
В продакшене я бы переключил на Redis одной строчкой в settings — код уже готов к этому.
Так я показал, что умею делать и простые, и масштабируемые решения.»